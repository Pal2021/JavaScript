const myname='jonas';
function first(){
    const age = 30;

    if(age>=30){
        const decade=3;
        var millenial=true;

    }
    function second(){
        const job='teacher';
        console.log(`${myname} is a ${age} old ${job}`);
        console.log(myname+"is a"+age+"old"+job);
    }
    second();
}
